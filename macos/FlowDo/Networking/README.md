No networking in Milestone 1. TaskStore is the seam for a future SyncedTaskStore.
A later API client must keep credentials in Keychain and must never upload input
activity. LocalTaskStore remains usable without the server or an account.
