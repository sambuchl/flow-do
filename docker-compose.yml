services:
  server:
    build: ./server
    ports:
      - "127.0.0.1:5000:5000"
    environment:
      SECRET_KEY: ${SECRET_KEY:-}
      DATABASE_URL: ${DATABASE_URL:-sqlite:///flowdo.db}
    volumes:
      - flowdo-data:/app/instance
volumes:
  flowdo-data:
